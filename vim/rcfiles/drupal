" Prompt for a module to install.
function! DrupalSingleInput(command, text)
  call inputsave()
  let input = input(a:text)
  call inputrestore()
  execute 'VtrSendCommandToRunner drupal ' a:command input
endfunction

" Drupal mappings to the vim-tmux-runner.
nnoremap <Leader>dcc :VtrSendCommandToRunner drupal cc all<CR>
nnoremap <Leader>dwd :VtrSendCommandToRunner drush watchdog-show<CR>
nnoremap <Leader>dce :VtrSendCommandToRunner drupal config:export<CR>
nnoremap <Leader>dci :VtrSendCommandToRunner drupal config:import<CR>
nnoremap <Leader>dmi :call DrupalSingleInput('module:install', 'Enter module to install: ')<CR>
nnoremap <Leader>dmu :call DrupalSingleInput('module:uninstall', 'Enter module to uninstall: ')<CR>

" vim:ft=vim
